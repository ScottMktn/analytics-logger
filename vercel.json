{
  "version": 2,
  "buildCommand": "cd frontend && npm --version && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm --version && npm install",
  "framework": "nextjs",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "frontend/$1"
    }
  ]
}
