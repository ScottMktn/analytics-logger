/** @type {import('next').NextConfig} */
const nextConfig = {
  // temporarily disable reactStrictMode to avoid double rendering while testing package
  reactStrictMode: false,
};

module.exports = nextConfig;
